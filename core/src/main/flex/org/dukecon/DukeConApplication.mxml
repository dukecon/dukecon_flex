<?xml version="1.0"?>
<s:Application xmlns:fx="http://ns.adobe.com/mxml/2009"
               xmlns:s="library://ns.adobe.com/flex/spark"
               creationComplete="onCreationComplete(event)">

    <fx:Metadata>
        [ResourceBundle("dukecon")]
    </fx:Metadata>

    <fx:Script>
        <![CDATA[
        import mx.events.FlexEvent;

        import org.dukecon.controller.ConferenceController;

        private var cc:ConferenceController;

        protected function onCreationComplete(event:FlexEvent):void {
            cc = ConferenceController.instance
        }

        public function get currentLocale():String {
            return resourceManager.getLocales()[0];
        }

        public function onExitClicked(event:MouseEvent):void {

        }
        ]]>
    </fx:Script>

    <s:ViewNavigator id="navigator" firstView="org.dukecon.views.TalksView"
                     width="100%" height="100%">
        <s:navigationContent>
            <s:Label text="lalala"/>
        </s:navigationContent>
        <s:actionContent>
            <s:Button label="exit" click="onExitClicked(event)"/>
        </s:actionContent>
    </s:ViewNavigator>

</s:Application>
